.col.mb-4
  .card
    - if food.photo.attached?
      = image_tag url_for(food.cropped_photo), class: 'card-img-top'
    .card-body
      %h5.card-title.d-flex.justify-content-between.align-items-center
        = food.name
        = link_to edit_food_path(food) do
          %i.fas.fa-edit
      - food.tags.each do |tag|
        %span.badge.badge-primary
          = tag.name