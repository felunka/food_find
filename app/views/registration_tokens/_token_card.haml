.col.mb-4
  .card
    .card-body
      %h5.card-title.d-flex.justify-content-between.align-items-center
        = token.auth_token
        = link_to registration_token_path(token), method: :delete do
          %i.fas.fa-trash
